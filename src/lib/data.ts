
import type { Event, User, Registration } from './types';

// This file is now deprecated as data is being fetched from Firebase.
// It is kept for reference but is no longer used in the application.

// Dummy user data
export const DUMMY_USERS: (User & { password?: string })[] = [];

// Dummy event data
export const DUMMY_EVENTS: Event[] = [];

// Dummy registration data
export const DUMMY_REGISTRATIONS: Registration[] = [];
